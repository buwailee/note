#!/bin/bash
# Author : buwailee
# Script follows here:
aplatex -synctex=1 manifold > /dev/null
echo "1 o.k."
zhmakeindex -q manifold.idx >/dev/null
echo "2 o.k."
aplatex -synctex=1 manifold > /dev/null
rm *.dvi *.ilg *.out
