% author: buwailee@nmhs
\ProvidesPackage{noteheader}[2016/06/17 Noteheader Package]

%% Package
\RequirePackage{amssymb, amsfonts, amsmath, amsthm, bm, mathrsfs}
\RequirePackage[b5paper, text={144mm, 208mm}, includehead, includefoot, hmarginratio=1:1, heightrounded]{geometry}
\def\pgfsysdriver{pgfsys-dvipdfm.def}
\RequirePackage{tikz}
\RequirePackage[all]{xy}

% fancy headers & footers
\RequirePackage{fancyhdr}
	\fancyhead{} % clear all header fields
	\fancyfoot{}
	%\renewcommand{\headrulewidth}{0.4pt}
	%\renewcommand{\footrulewidth}{0.4pt}
	\pagestyle{fancy}
	% \renewcommand{\chaptermark}[1]{\markboth{\thechapter}{}} %{\markboth{\MakeUppercase{#1}}{}}
	\renewcommand{\sectionmark}[1]{\markright{\thesection.\ #1}}

	% \renewcommand\thechapter{\Roman{chapter}} 
	% \renewcommand\thesection{\arabic{section}}
	% \newcommand{\ssection}[1]{%
	%   \section[#1]{\centering\normalfont\scshape #1}}
	% \newcommand{\ssectionstar}[1]{%
	%   \section*[#1]{\centering\normalfont\scshape #1}}
	% \newcommand{\ssubsection}[1]{%
	%   \subsection[#1]{\raggedright\normalfont\itshape #1}}
	% \newcommand{\ssubsection}{\subsection}
%%

%% Option
\DeclareOption{article}{
	\fancyhead[L]{\sf \rightmark}
	\fancyhead[R]{\sf \thepage}
}
\DeclareOption{book}{
	\fancyhead[LO,RE]{\sf \rightmark}   % [LO, RE]
	\fancyhead[RO,LE]{\sf \thepage}
}

\newif\if@zh\@zhfalse
\DeclareOption{en}{\@zhfalse}
\DeclareOption{zh}{\@zhtrue}

\newif\if@aptex\@aptexfalse
\DeclareOption{aptex}{\@aptextrue}
\DeclareOption{xetex}{\@aptexfalse}

\ExecuteOptions{article, en, xetex}
%%

%% Relax
\ProcessOptions\relax

\if@zh
	\RequirePackage{indentfirst}
\fi

\if@aptex
	\geometry{top=36mm}
	% b5paper=176mm*250mm
	\pdfpagewidth=176truemm
	\pdfpageheight=250truemm
	\oddsidemargin=16truemm
	\evensidemargin=16truemm
\else
	\geometry{top=10mm}
\fi
%%

\endinput