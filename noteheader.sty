% author: buwailee@nmhs
% \ProvidesPackage{noteheader}[2016/05/26 Noteheader Package]

\RequirePackage{amssymb,amsfonts,amsmath,amsthm,bm,mathrsfs,tikz}
\RequirePackage[left=19mm,text={148mm,200mm},paperwidth=186mm,paperheight=250mm,includehead,vmarginratio=1:1]{geometry}
\RequirePackage[all]{xy}

% fancy headers & footers
\RequirePackage{fancyhdr}
	\fancyhead{} % clear all header fields
	\fancyfoot{}
	%\renewcommand{\headrulewidth}{0.4pt}
	%\renewcommand{\footrulewidth}{0.4pt}
	\pagestyle{fancy}
	% \renewcommand{\chaptermark}[1]{\markboth{\thechapter}{}} %{\markboth{\MakeUppercase{#1}}{}}
	\renewcommand{\sectionmark}[1]{\markright{\thesection.\ #1}}

	% \renewcommand\thechapter{\Roman{chapter}} 
	% \renewcommand\thesection{\arabic{section}}
	% \newcommand{\ssection}[1]{%
	%   \section[#1]{\centering\normalfont\scshape #1}}
	% \newcommand{\ssectionstar}[1]{%
	%   \section*[#1]{\centering\normalfont\scshape #1}}
	% \newcommand{\ssubsection}[1]{%
	%   \subsection[#1]{\raggedright\normalfont\itshape #1}}
	% \newcommand{\ssubsection}{\subsection}

\DeclareOption{article}{
	\fancyhead[L]{\sf \rightmark}
	\fancyhead[R]{\sf \thepage}
}
\DeclareOption{book}{
	\fancyhead[LO,RE]{\sf \rightmark}   % [LO, RE]
	\fancyhead[RO,LE]{\sf \thepage}
}
\DeclareOption{en}{}
\DeclareOption{zh}{\let\@afterindentfalse\@afterindenttrue\@afterindenttrue}

\ExecuteOptions{article,en}
\ProcessOptions\relax
\endinput